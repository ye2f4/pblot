# 2026-07-21 工作日志

## 生产 /app 跳转修复 + B站完善信息 + 注册预留区 + 加载 GIF + 部署记录

### 1. /app 跳转 404（根因：客户端 SPA 跳转未触达 Vercel rewrite）
- 主站所有 `/app/*` 链接改为整页 `<a href>`（原用 Docusaurus `<Link>` 客户端跳转 → 请求不到达 Vercel `/app/*` rewrite → Docusaurus 自身 404）。
- 已改文件：`src/theme/Navbar/index.tsx`、`src/theme/Footer/index.tsx`（DLink 内 `to` 以 `/app` 开头时渲染 `<a>`）；以及 `src/components/TopBanner/index.js`（login/register/profile）、`MainContentTop`（抽贴）、`FriendsAndAbout`（查看全部）、`src/pages/contribute.js`、`signin.js`、`about.js`。

### 2. 布局混乱（根因：assetPrefix 生产为空）
- `apps/next-app/next.config.mjs`：assetPrefix 之前只读 `NEXT_PUBLIC_VERCEL_URL`（Vercel 不注入该变量），生产取空→相对路径→经主站代理时 `/_next/static/*` 落到主站→无样式。已加 `VERCEL_URL`（构建期自动注入）兜底，自动修复，无需手配。可选更稳：设 `NEXT_PUBLIC_ASSET_PREFIX=https://<next-app 稳定域名>`。当前环境无 VERCEL_TOKEN，未用 CLI 配置。

### 3. B站登录后完善信息（流程同注册）
- 重建 `apps/next-app/app/bilibili-callback/page.tsx`（此前 0 字节）：显式解析 fragment→setSession→查 `profiles.username`，缺失跳 `/complete-profile`，否则 `/profile`。
- 新建 `apps/next-app/app/complete-profile/page.tsx`：镜像注册 step2，必填 username（3-20 位查重）+ 预填 B站昵称/头像 + 可选签名/性别，upsert profiles。
- 新建 `apps/next-app/components/ProfileGuard.tsx` 并接入 `app/layout.tsx`：已登录但无 username 且不在白名单(login/register/complete-profile/bilibili-callback)→跳 complete-profile。
- 主站 `src/pages/bilibili-callback.js`：登录后缺 username 则整页跳 `/app/complete-profile`（覆盖主站 TopBanner 入口）。
- B站登录入口：App=`/app/bilibili-callback`（login/page.tsx）；主站=`/bilibili-callback`（useAuth.js）。

### 4. 注册页预留区（用户选：精选内容/插画）
- `apps/next-app/app/register/page.tsx` 改两栏，右侧 `<aside>` 放欢迎插画(unsplash)+3 张精选内容卡片(论坛/投稿/朋友圈)，step3 成功页不显示。结构语义化易替换。

### 5. 加载占位统一 LOADING.gif
- 结论：next-app 无 public 目录，普通 `<img src="/img/LOADING.gif">` 不会加 basePath，浏览器请求根域 `/img/LOADING.gif`（主站 Docusaurus static 提供），生产与 dev:all(经3000代理) 均成立，**无需复制到 next-app**。
- 已替换（文字→GIF `width=56 opacity=.92`）：next-app 的 submissions(2)/moments/leaderboard/friends/forum/chat/capsule；主站 signin/changelog/hardware/snippets/pcb(293)/visit-map(578,653)/index(2处Suspense)/shake-network(559,750)/admin(588)/WeatherWidget。
- 保留文字：pcb:384(刷新按钮状态)、shake-network:611(`<option>`)、各处“加载失败”错误文案。

### 6. 双轨部署 / GitHub Actions（用户选：加记录工作流）
- 根因：`.github/workflows/` 原只有 docs-review/local-groups-review/supabase-keepalive，**无 `on:push` 工作流**→双轨 push 不产生 Actions 记录；Vercel 经自家 GitHub App 感知 push 自动构建（两套独立系统）。属预期。
- 新建 `.github/workflows/deploy-record.yml`：on push(master/main)+dispatch，仅写运行摘要(提交信息/git log)，不 install/build，避免与 Vercel 重复。
- 注：`gh` CLI 在本机未安装（github 技能脚本无法直接跑）。

### 7. /app 不生效 + Vercel 构建 ERROR（根因：rebase 冲突标记被提交进 master）
- 现象：推送后 `next-app` 与 `pblot-2q56` 均 `ERROR`，Vercel 报 `ERR_PNPM_JSON_PARSE`（pnpm 解析 package.json 失败）。
- 根因：此前 `git pull --rebase` 出现 `AA`/冲突，我误用 `git add -A` 把**带 `<<<<<<<`/`=======`/`>>>>>>>` 标记的文件**（约 39 个，含 `packages/ui/package.json`、`chat/page.tsx`、`ui.css` 等）整批提交进 `ef19e751`，污染了 master。且 rebase 重命名检测使部分文件两侧内容被搅乱（如 chat/page.tsx 首行变成 SitePolish 内容）。
- 修复：
  1. 用干净提交 `54107eca`（= 我的 /app 改动提交，`>>>>>>>` 标记里的源提交）整体恢复：`git diff --name-only 54107eca HEAD` 取差异文件，`git show 54107eca:<path>` 覆盖写回磁盘 → 39 个文件全部恢复为干净版本（0 失败）。
  2. 校验全仓无冲突标记（0 处）、9 个 JSON 文件合法（0 bad）。
  3. 新提交 `e4aea71f` 推上 master → 两项目自动重建 → `next-app` 与 `pblot-2q56` 均 `READY`，ERROR 消失，`/app` 改动上线。
- 教训：`git add -A` 遇 rebase 冲突会暂存带标记文件，**务必先 `git diff` 检查再提交**；自动 `$2` 去标记不可靠（rebase 重命名会错配），应直接从源提交 `git show` 恢复。
- Vercel 关键标识（team `shaorin-zhao-s-projects` = `team_cmkrxKsOusMzGqJuDFh84ayZ`）：
  - `/app` 项目名 `next-app`，id `prj_XGgE5QIYs4oPWHhyG3WsHknBQqd1`，域名 `next-app-mocha-three.vercel.app`，根目录 `apps/next-app`，已连 git（push master 自动重建）。
  - Deploy Hook（已写入 `scripts/deploy.config.mjs` 的 `NEXT_APP_DEPLOY_HOOK`）：`https://api.vercel.com/v1/integrations/deploy/prj_XGgE5QIYs4oPWHhyG3WsHknBQqd1/uY0sBclNjq`
  - 主站项目 `pblot-2q56` id `prj_njoiVtyLCjt9hY8YqlwR7oimd2sQ`（docusaurus，仓库根）。

### 8. /app 所有数据页永久卡「加载中」（根因：getUser 挂起无兜底）
- 现象：monoblog.cc.cd/app 下 chat/friends/个人中心/完善信息等全部数据页卡在「加载中」，登录与否都一样；论坛页正常。
- 排查：forum 走 anon `select`（不调 getUser）正常 → Supabase REST/CORS 均正常（`/auth/v1/user` 仅带 anon key 时 403 属预期；CORS 放行 monoblog.cc.cd）。`profiles/groups/group_members/messages` 的 RLS 均为 `USING(true)`，非递归。故非 RLS/网络问题。
- 真正根因：`supabase.auth.getUser()/getSession()` 在浏览器会话失效（如旧 cookie/过期 JWT）触发静默 token 刷新且请求永不 settle，而各页面**无 try/catch 也无超时** → `loading` 永远不翻 false → 永久卡住。论坛页不调 getUser 故幸免。
- 修复（commit `5d92e727`，本地提交未推送）：
  - 新增 `apps/next-app/lib/supabase/safe.ts`：`safeGetUser()`/`safeGetSession()` 带 **8s 硬超时（Promise.race）** + 失败兜底（降级为未登录并 `signOut()` 清掉失效会话），杜绝永久挂起。
  - 统一替换调用点：`useUser`（覆盖 moments 等）、`AppNav`、`ProfileGuard`、`chat`、`capsule`、`complete-profile`（无兜底→重写为带 finally）、`ProfileEditor`、`bilibili-callback`、服务端 `profile/page.tsx`（SSR 侧加超时）。
  - `friends` 页是「友情链接」目录（查 `friend_links`，已有 try/catch/finally），非 chat 内好友列表，不受影响。
- 验证：本地 `next dev -p 3100` 各页均 200 编译通过、无报错浮层；lint 0 错误。
- 教训：浏览器端任何 supabase 异步调用都**必须**有超时/错误兜底，绝不能直接 `await` 裸调用后依赖 `loading` 翻转。
- 注：本机 agent shell 的 pnpm store 路径损坏（`mkdir '\\?'` ENOENT），`pnpm dev` 起不来；可直接 `node node_modules/next/dist/bin/next dev` 绕过。
