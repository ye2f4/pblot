# 2026-07-20 工作日志

15. **歌词移入公告栏 + 移除本地播放（musicContext 联动）**
  - **歌词显示迁移到公告栏**：新建 `src/utils/musicContext.js`（`MusicContext`+`MusicProvider`+`useMusic`，持有 `{isPlaying,currentSong,lyrics,activeLyric}`+`setShare`）。`src/pages/index.js` 用单个 `<MusicProvider>` 包裹首页的 `TopBanner` 与 `MainContentTop`（二者为兄弟组件，同实例共享）。`TopBanner` 内新增 `useEffect` 把播放状态/当前歌/歌词/当前歌词行发布到 context；**移除挂件内「词」按钮与展开歌词面板**（及 `lyricsOpen`/`lyricsLoading` 状态）。`MainContentTop` 用 `useMusic()` 读取：`share.isPlaying && 歌词存在 && activeLyric>=0` 时公告栏容器改为 **居中静态**显示 `lyrics[activeLyric].text`，否则维持原 **跑马灯公告**（`texts.notification`）。配色/容器样式不变。
  - **admin 可改歌单 ID**：`src/config/adminConfigSchema.js` 的 `music.mode` 仅保留 `netease` 选项（移除 local）；歌单/歌曲 ID 编辑走已有 `music.playlistId`(text) 与 `music.songIds`(list，admin.js 已支持文本↔数组)。用户可在 admin「首页板块开关」分组直接编辑 `music.songIds` 并即时生效（`SiteConfigProvider.applySiteConfig` 按点路径合并）。
  - **移除本地播放（local MP3）**：`TopBanner` 的 `mode` 硬编码 `'netease'`，删除 local `else` 分支（原读 `music.mp3List`）及依赖数组中的 `mode`/`mp3List`；连播显隐条件由 `mp3List.length` 改为 `songIds.length`；`src/data/siteData.json` 删除 `music.mp3List`。app 端（next-app）不用 TopBanner，不受影响。lint 0 错。
  - **验证**：`pnpm dev:all` 冷启动 → 首页默认公告跑马灯；点播放公告栏变居中歌词当前行并随进度更新；暂停恢复公告；TopBanner 无「词」按钮/面板；admin 改 `songIds` 生效。


0. **音乐播放不了根因=CSP 缺 media-src / dev:all 端口透传 bug（当日后续修复）**
  - **歌曲能显示但放不出**：`docusaurus.config.js` 的 CSP 没有 `media-src` 指令，`<audio>` 源回退到 `default-src 'self'`，网易云 `music.163.com` 及 302 跳转的 CDN 被拦。歌曲元数据走 API（`connect-src` 已放行 :3009）所以能列表显示。修复：加 `"media-src 'self' data: blob: https://*.music.163.com https://music.163.com https://*.music.126.net https://*.music.127.net http://*.music.126.net http://*.music.127.net"` + dev 追加 ` http://localhost:3009`。外链 `curl -IL` 验证 302→audio/mpeg 200 且 `ACAO:*`，URL 本身可达。
  - **`pnpm start -- --port 3002` 报 ENOENT lstat 'F:\my-forum\--port'**：pnpm 把 `--` 后参数当路径透传给 docusaurus。修复：package.json 新增 `"start:proxy": "docusaurus start --port=3002"`（内联参数），`dev:all` 的 site 进程改用 `pnpm start:proxy`。


1. **setLyrics is not defined 修复**
  - 根因：之前给 `MusicWidget` 加歌词功能时，三行状态声明（`lyrics`/`lyricsOpen`/`lyricsLoading`）实际未写入 `src/components/TopBanner/index.js`，第 123 行仍是孤立的 `const [isLoading, setIsLoading]=useState(true)`；而 `useEffect` 已调用 `setLyrics*` → 运行时报 not defined。
  - 修复：在 `isLoading` 状态后补上 `const [lyricsOpen,setLyricsOpen]=useState(false); const [lyrics,setLyrics]=useState(null); const [lyricsLoading,setLyricsLoading]=useState(false);`。lint 0 错。
  - （歌词面板功能本身已在 07-19 完成：解析 LRC、netease 按歌曲 id 拉词、折叠式面板 max-h-[112px]、控制栏"词"按钮、build 保留 id。）

2. **/app 404 Vercel 部署指导（用户问"先回答这个怎么做"）**
  - 架构：主站 /app/* → rewrite 到 Edge 函数 `api/app/[...path].js` → fetch `NEXT_APP_ORIGIN + /app + 路径`。该函数 `NEXT_APP_ORIGIN` 兜底 `https://next-app-mocha-three.vercel.app` 不存在 → 404。结论：不是"nextjs 没绑主域名"，是 next-app 没独立部署 / 主项目没设环境变量。
  - 步骤：A) Vercel 用同一仓库新建项目，Root Directory=`apps/next-app`、Framework=Next.js、Deploy 拿域名（可绑自定义域）。next-app 已设 `basePath:'/app'`，勿去掉。B) 主项目 Settings→Environment Variables 加 `NEXT_APP_ORIGIN=https://<next-app域名>`，勾 Production，保存后 Redeploy 主项目。C) 验证 /app 不再 404；仍 404 则查 Edge 函数日志的 target。
  - 可优化：把兜底域名改成"缺失即 500"，避免静默 404（用户未要求，待确认）。

3. **音乐播放器"版权限制"根因 + 修复（对比 XinghuisamaBlogs/F:/XinghuisamaBlogs）**
  - **为什么它能播、我的不行**：XHBlogs 有两条路径——`CloudPlayer.tsx` 直连 `api.injahow.cn/meting`（和我原来一样，报"音乐流被拦截，可能是版权限制"）；而真正能播的 `MusicProvider.tsx` 走**自己的服务端代理** `/api/music`（Node Route）`route.ts`：带 `Referer:https://music.163.com/` + 真实 UA 调 163 官方 `api/song/detail`，并用 `media/outer/url?id=X.mp3` 外链（浏览器 `<audio>` 直连可播，无需 CORS/Referer）。**结论：直连 meting 对版权曲返回的 url 多为空值 → 播放列表为空 → 报版权拦截。根因是缺服务端代理。**
  - **修复**：新增 `api/music.js`（Edge 函数，复用现有 api/ 由 Vercel 托管，与主站/APP 同域，路径无关），复刻 XHBlogs 服务端代理逻辑并扩展支持 `?ids=` 与 `?pid=`(歌单)，每首一并返回 lrc。
  - `MusicWidget`(TopBanner/index.js) netease 分支改为 fetch `/api/music`，字段映射含 `lyrics: parseLrc(lrc)`；歌词 effect 改用 `currentSong.lyrics`，**去掉对 meting 的直连依赖**。siteData 仍是 `mode:"netease", playlistId:"3778678"`，现经代理可正常播放+歌词。两个文件 lint 0 错。

4. **XinghuisamaBlogs 可借鉴功能盘点（用于丰富 my-forum）**
  - 交互亮点：CyberCat（AI 暹罗猫，framer-motion 拖拽+Gemini `/api/chat`，需 GEMINI_API_KEY）、DanmakuBackground（全局弹幕背景，纯 CSS）、SplashScreen（开场动画）、ClickEffect（点击粒子）、PageTransition。
  - 氛围特效：Sakura/GlobalSnow/Fireflies/WindyGrass/WeatherEffect/BackgroundSlider（视觉氛围）。
  - 内容展示：LatestPostsCarousel/LatestChatterCarousel（轮播）、ProfileCard/SiteDashboard（个人卡+数据面板）、Timeline（时间线）。
  - 互动模块 app/tree：DijiangModel/AlchemyLab/CreativeWorkshopClient/OperatorRecreation（趣味互动页）。
  - 评论：Comments/LabComments/MomentComments + Gitalk（需 clientID/secret）。
  - 工具箱：components/toolbox/ + GlobalToolbox（小工具集合）。
  - my-blog-manager：独立 CMS 后台（重型，可作投稿/管理后端，但迁移成本高）。
  - 移植注意：这些多为 Next.js + framer-motion + Tailwind 组件；主站是 Docusaurus(React) 也能跑 React 组件，但 framer-motion 需加依赖；导航/页脚走共享包 @mono/ui，新组件应以独立 React 组件形式挂到页面/布局，避免污染共享包。

5. **全站精致度特效（SplashScreen+ClickEffect+PageTransition）+ 两个 gif 替换**
  - **主题确认**：站点品牌色是**绿**（亮 `#1a8a50`=hsl(152 75% 28%)，暗 teal hsl(132 84% 68%)，奶油/深蓝底），之前 MusicWidget 用靛蓝是偏色；特效统一用绿色。
  - 新建 `src/components/SitePolish.js`：`SplashScreen`（开场 logo+绿环旋转，sessionStorage 每会话一次，reduced-motion 跳过，1.95s 淡出）、`ClickEffect`（全局 pointerdown 生成绿色粒子，pointer-events:none 浮层，reduced-motion 跳过）、`PageTransition`（useLocation 以 pathname 为 key 触发 0.32s 淡入上移）。
  - `src/theme/Root.js` 挂载：`<SitePolish/>`（浮层）+ `<PageTransition>{children}</PageTransition>`。Root 是 Docusaurus 全局根，覆盖所有主站页面。
  - **gif 替换**：`static/img/LOADING.gif` 替换 7 处"⏳/加载中..."文字（TagCloud/RankList/FriendsAndAbout×2/ContributionHeatmap/CarouselSection/TopBanner 登录）；`static/img/404.gif` 替换 404 页大搜索图标（swizzle `src/theme/NotFound/Content.js`，绿色标题+返回首页）。
  - `PageLoading.js` 顶部进度条由蓝 `#007bff` 改为绿 `hsl(152 75% 28%)`。
  - 全部 lint 0 错。特效仅挂主站（Docusaurus）；`/app`(Next) 若需复用可把 SitePolish 加到其 layout。未 build，等热更新验收。

6. **部署后两个构建错误 + /app 错乱 HTML 修复**
  - **`SitePolish.js` 写入失败（空文件）**：上一轮 `write_to_file` 实际没把内容落盘，文件为空、无 export；`Root.js` 的 `import SitePolish, { PageTransition }` 导入失败 → 报 "Element type is invalid ... Check the render method of Root"。已**重新写入完整内容**（SplashScreen/ClickEffect/PageTransition，纯 React+CSS，无 framer-motion 依赖）。
  - **PageTransition 实现改为劫持 history**（非 useLocation）：Docusaurus `Root` 在 Router 之外，用 useLocation 会报 "useLocation() may be used only in Router context"；且最初用 key 重挂载 children 会丢失页面状态。改为 `ref + 重播 CSS 动画`，仅淡入（纯 opacity）不重挂载。
  - **`onCloseAll is not defined`**：`WarningModal/index.js` 第202行误用 `onCloseAll`，实际定义是 `closeAll`（第99行）。已改为 `closeAll`。
  - **`/app` 错乱 HTML 根因**：next-app `basePath:'/app'`，HTML 经主站代理返回，但页面引用的 CSS/JS 是 `/app/_next/static/...`。**Vercel 对 `/_next/*` 是平台保留路径，`vercel.json` 的 rewrite 不会把它转发给代理函数** → 静态资源在主站 404 → 只剩无样式 HTML 骨架（导航+页脚+加载中）。
  - **修复**：next-app `next.config.mjs` 加 `assetPrefix`，让静态资源走绝对域名，绕过主站代理的 `/_next` 限制。取值优先级：`NEXT_PUBLIC_ASSET_PREFIX`（自定义域）> `NEXT_PUBLIC_VERCEL_URL`（Vercel 自动注入的部署域名，兜底，部署即生效）。在 Vercel 重新部署 **next-app** 即可让资源直连 next-app 域名，/app 恢复正常样式。**注意**：仅改 next.config 后未重新部署 next-app 仍会错乱；若用自定义域需设 `NEXT_PUBLIC_ASSET_PREFIX`。
  - 两个主站文件（SitePolish.js / WarningModal）与 Root.js lint 0 错。

7. **补修：NotFound/Content.js 当初也未落盘（同 SitePolish 写入失败模式）**
  - 核查发现 `src/theme/NotFound/Content.js` 是空文件（与 SitePolish.js 同一批 write 失败），全 src 搜 `404.gif` 为 0 → 第5项记的"404.gif 替换 404 页"实际没落实。已重写 `Content.js`（404.gif 主视觉 + 绿色标题"页面未找到" + 返回首页 `@docusaurus/Link`）。
  - 另发现 `src/theme/NotFound/Content/` 目录含默认 `index.js`（Docusaurus 原始 404：🔍 大搜索图标 + 蓝按钮），与 `Content.js` 同名模块歧义；Webpack 解析 `@theme/NotFound/Content` 优先 `Content.js` 文件，已 `delete_file` 删掉 `Content/index.js` 消除歧义。
  - 精确确认 7 处 `/img/LOADING.gif` 替换均落实（TagCloud/RankList/ContributionHeatmap/TopBanner/TagCloud 各1，FriendsAndAbout 2）。
  - **教训**：本轮早期那一批 write_to_file 部分静默失败（SitePolish.js、NotFound/Content.js 落盘为空），之后重写均已成功确认。今后再用 write_to_file 创建较大文件后，应立刻 read 回确认非空。

8. **构建报错 `Cannot find module '@theme/NotFound/Content'` 修复**
  - 第7项删了 `Content/index.js` 但**残留空 `Content/` 目录**，与 `Content.js` 同名 → Docusaurus 的 `@theme` 别名解析歧义，编译报找不到模块（DocRoot 与 NotFound/index.js 两处报错）。
  - 已 `Remove-Item .../NotFound/Content -Recurse` 删除空目录，仅留 `Content.js` + `index.js`。别名 `@theme/NotFound/Content` 现唯一指向 `Content.js`，编译报错消除。
  - **教训**：删 `xxx/index.js` 同时要删掉空的 `xxx/` 目录，否则同名 `.js` 文件会被目录干扰导致别名解析失败。

9. **`@theme/NotFound/Content` 报错仍未消除（第8步后复现）——根因是别名缓存，非文件**
  - 第8步删空 `Content/` 目录后报错仍在。确认 `Content.js` 一直存在且 `export default`。进一步读默认主题 `DocRoot/index.js` 第15行发现它**也** `import NotFoundContent from '@theme/NotFound/Content'` → 该别名必须存在，且之前别名映射在 dev server 启动时已生成、指向已删的 `Content/index.js`，HMR 不会重建别名。
  - **真正修复**：`Remove-Item f:/my-forum/.docusaurus -Recurse -Force` 清除 Docusaurus 缓存，让下次 `pnpm dev` 重启时重新扫描 `src/theme`、把 `@theme/NotFound/Content` 正确指向 `Content.js`。
  - **关键教训**：改 `src/theme` 目录结构（增删文件/目录）后，**dev server 必须重启**（不能只靠 HMR），因为 `@theme` 别名映射是 webpack 启动时一次性扫描生成的，HMR 不重建它。遇到 `Cannot find module '@theme/xxx'` 且文件确实在位时，先清 `.docusaurus` 缓存再重启。

10. **误删运行中的 `.docusaurus` 引发 `@generated` 级联报错（第9步后）**
  - 第9步在 **dev server 仍在运行时** 执行 `Remove-Item .docusaurus`，导致 server 进入半重新生成状态：`.docusaurus/registry.js` 引用尚未写好的 `@generated/@easyops-cn/docusaurus-search-local/...` 文件（搜索插件），冒出一批 `@generated` 找不到模块的报错；`@theme/NotFound/Content` 也仍在（别名映射同样没写全）。
  - 检查确认当时 `.docusaurus` 已被删空/不存在（ENOENT）。当前为干净状态。
  - **正确做法（已告知用户）**：先**彻底停掉 dev server**（Ctrl+C），再冷启动 `pnpm dev`/`pnpm dev:all`，让 Docusaurus 完整重建 `.docusaurus`（含 `@generated` 与 `@theme` 别名）。绝不要在 server 运行时手删 `.docusaurus`。
  - **教训**：改 `src/theme` 后必须"停服→冷启动"；手删 `.docusaurus` 只能在 server 已停时进行，否则必触发半生成损坏。冷启动可同时解决 `@generated` 与 `@theme` 两类报错。

11. **共享包 @mono/ui 两处导航栏 bug 修复（onNavigate + 高度裁切）**
  - **`onCloseAll/onNavigate is not defined`**：`packages/ui/src/SiteHeader.tsx` 的 `MobileItem` 组件在下拉子项 `onClick={onNavigate}`（第138/145行）引用了未定义的 `onNavigate`，点移动端子菜单项即崩。修复：给 `MobileItem` 加 `onNavigate: () => void` 参数；普通项（有 href 的 `<a>` 与内部 `<L>`）也补 `onClick={onNavigate}`；`SiteHeader` 渲染 left/rightItems 时传 `onNavigate={() => setMobileOpen(false)}`（点击导航即关闭侧边栏）。
  - **导航栏下边线过高、绿色按钮/搜索框溢出到边线外**：根因 `src/css/custom.css` 设 `--ifm-navbar-height:4.5rem`，Infima 把 `.navbar` 强制 `height:72px` 固定裁切，而 `ui.css` 的 `.navbar` 没重置 height。修复：`ui.css` 的 `.navbar` 加 `height:auto !important; min-height:var(--ifm-navbar-height,4.5rem);`，内容更高时自动撑开，下边线落真实底部。
  - 两文件 lint 0 错。@mono/ui 同时供 3000 主站 + 3001 app，两端一并受益。未 build，热更新验证。

12. **netease 修复"本地看不到效果"根因 + 本地 /api 验证方案落地**
  - **根因两层**：① 这轮一直在修的 `@theme`/`@generated` 构建报错让全站编译失败，什么都渲染不出；② 更关键——`/api/music` 是 Vercel Edge 函数（`api/music.js`），本地 `pnpm dev:all` 没有进程提供 `/api/*`，`MusicWidget` 的 netease fetch 必然 404 → 播放列表为空 → 看起来没修好。配置（siteData `mode:"netease", playlistId:"3778678"`）与代码均正确，只是本地缺服务端。
  - **先确认约束**：读 `node_modules/@docusaurus/core/lib/commands/start/webpack.js`，Docusaurus 3.10.1 的 `createDevServerConfig` 从头构造 devServer 配置，**不把 `siteConfig.beforeDevServer`/`afterDevServer` 透传**，故"在 docusaurus.config.js 挂中间件"在 3.10 走不通 → 改用独立本地 API 服务 + 环境感知基址。
  - **改动（生产不受影响）**：
    1. 新建 `api/music-core.mjs`：抽出 `getSong`/`getPlaylistIds`/`fetchMusic({ids,pid})` 纯函数，Vercel 端与本地端共用（用 `.mjs` 显式 ESM，本地 `node` 也能 import；根 package.json 是 CJS，`.js` 会被当 CJS）。
    2. 改写 `api/music.js`：薄 Edge 包装，`import { fetchMusic } from './music-core.mjs'`，行为不变。
    3. 新建 `scripts/dev-api.mjs`：`node:http` 监听 `:3009`，处理 `GET /api/music?ids=&pid=`，复用 `music-core.mjs`，响应带 `Access-Control-Allow-Origin:*`。
    4. `package.json`：`dev:all` 用 concurrently 起 3 进程（site/app/api），新增 `node scripts/dev-api.mjs`；另加 `dev:api` 脚本。
    5. `src/components/TopBanner/index.js`：`MusicWidget` 加 `API_BASE = process.env.NODE_ENV==='production' ? '' : 'http://localhost:3009'`，两处 netease fetch（`?ids=`/`?pid=`）改用 `${API_BASE}/api/music`。
    6. `docusaurus.config.js`：CSP `connect-src` 在 `NODE_ENV!=='production'` 时追加 `http://localhost:3009`（生产不含，规避跨源被 CSP 拦）。
  - **校验**：`node --check` 三个 .js/.mjs 全过；TopBanner lint 0 错。
  - **用户验证步骤**：彻底停服→冷启动 `pnpm dev:all`（package.json 改了必须重启）；:3000 应见开场动画/点击粒子/切页淡入（SitePolish 已挂 Root.js，SplashScreen 用 sessionStorage 每会话一次）；播放器 netease 应能拉到 3778678 歌单并播放+歌词；`vercel deploy` 后线上 `/api/music` 同源生效。`node --check` 已确认脚本可解析。

13. **用户偏好：不要并行写入文件**
  - 用户明确要求：执行文件写入（`write_to_file`/`replace_in_file`）时**不要并行/批量**发起多个写操作，应逐个顺序执行。原因：此前在同一轮并行写多文件曾出现 "No result found" 写入失败（如 `api/music-core.mjs` 首次写入被中断）。记忆写入（`update_memory`）也单独、顺序进行。自 2026-07-20 起生效（已记入 long-term MEMORY）。

14. **四项修正（音乐歌单/开场动画/本地 /app 路由代理）**
  - **音乐歌单照搬 XinghuisamaBlogs**：`src/data/siteData.json` 的 `music` 改为 `mode:"netease", playlistId:"", songIds:["1809646618","3361076230","1859390262"]`（XHBlogs `siteConfig.cloudMusicIds`）。原 `playlistId:"3778678"` 整张歌单作废。理由：XHBlogs 用 `ids` 单曲直链（与 my-forum 现有 `/api/music?ids=` 逻辑完全一致），而 3778678 整张歌单经代理拉到的歌曲多为版权受限/空 url → 报"音乐流被拦截"。TopBanner 在 `songIds` 非空时传 `ids`，dev-api(:3009)→`fetchMusic({ids})`→`getSong` 返回 `outer/url` 外链，字段 `id/name/artist/url/lrc` 已与 TopBanner 映射匹配。
  - **SplashScreen 延长到 2s + 进度条 + System initializing**：`src/components/SitePolish.js` 计时器 1500→2000ms、移除 1950→2450ms；加载环下方加宽 200 进度条（`splashProgress 2s linear forwards`）+ 绿色英文 "System initializing"。lint 0 错。
  - **本地 /app 路由反向代理（最贴近生产）**：新建 `scripts/dev-proxy.mjs`（纯 Node `http`，无第三方依赖），监听 **:3000**：路径以 `/app` 开头 → next-app **:3001**（`basePath:/app`），其余 → docusaurus **:3002**；`upgrade` 事件双向 pipe 支持 WebSocket，保留两端 HMR。根 package.json `dev:all` 改为 4 进程（proxy,site,app,api）：`node scripts/dev-proxy.mjs` + `pnpm start -- --port 3002` + `pnpm --filter next-app dev` + `node scripts/dev-api.mjs`。现开 `localhost:3000` 即等价于生产 /app 路由，主站导航栏 `/app/*` 不再跳到裸 docusaurus。
  - **本地 dev 端口约定更新**：代理 **3000**、docusaurus **3002**、next-app **3001**、dev-api **3009**。（旧记忆"主站 :3000 / app :3001"仅对线上/独立 `pnpm start` 成立；dev:all 下主站已迁 3002，3000 让给代理。）

16. **修复 "Rendered more hooks than during the previous render" + 双轨部署加入 git commit -m**
  - **hooks 报错根因**：`src/components/TopBanner/index.js` 的 `MusicWidget` 在 `if (isLoading) return` 与 `if (playlist.length===0) return` 两个 early return **之后**才调用 `useMusic()`（原第248行），违反 Hooks 规则 → 加载/空列表态少调用一次 hook。修复：把 `const { setShare } = useMusic();` 提到组件顶部（audioRef 之后、所有 early return 之前），删除原处重复调用。
  - **顺带修重复公告栏**：`src/pages/index.js` 之前用单个 `MusicProvider` 包裹时**又多渲染了一个 `MainContentTop`**（provider 内一份 + main-content 内原版一份）→ 首页出现两条公告。现改为：单一 `<MusicProvider>` 包裹 `TopBanner` 与真正的 `MainContentTop`（main-content 容器内那一份），删掉重复的那份。两组件共享同一 context 实例。setShare 仅在歌词行切换/播放态变化时触发，不会每音频 tick 重渲整页。
  - **双轨部署加入真实 git commit**：`scripts/github-push.mjs` 新增 `gitCommitAndPush(message)`（execFileSync 数组参数，安全）。A 线改为「git 优先」：`git add -A` → 有改动则 `git commit -m "<msg>"` → `git push origin HEAD:<branch>`；成功则跳过 GitHub API 建树（避免重复提交），失败/禁用/git 缺失则回退原 GitHub API 推送。提交信息来自 `runDeploy({message})`，默认 `cfg.message`（'deploy: 自动同步'）。配置 `GITHUB_GIT_COMMIT=0/false/no` 可关 git 改纯 API。提交成功但 push 失败会 `git reset --soft HEAD~1` 保证工作区干净。
  - **UI 串联**：`src/components/DevDeployPanel/index.js` 新增「提交信息」输入框（占位默认 message，提示用 `update:Vx.x` 激活 AI 更新日志），部署时 `POST /api/deploy` 带 `{message}`；`plugins/deploy-ui-plugin.js` 的 `/api/deploy` 中间件改为 `async` 并解析请求体把 message 传给 `runDeploy`；`getDeployStatus` 现返回 `gitEnabled`/`message` 供面板展示。
  - **激活 AI 更新日志说明**：`get-commit-desc.sh` 仅对 `update:Vx.x` 类提交生成 VERSION/TYPE/DESC（内置 AI 摘要）。现部署会真实写入 git 提交，提交信息用 `update:Vx.x` 即能被仓库历史/CI 识别。注意：`get-commit-desc.sh` 当前未被任何 CI workflow 调用（`.github/workflows` 无引用），若需自动落库 `update_logs` 还需另接 CI/Edge 函数——本次仅补齐"git commit"这一步。
  - 校验：`node --check` 通过 github-push.mjs 与 deploy-ui-plugin.js；TopBanner/MainContentTop/DevDeployPanel/index.js 与 index.js lint 0 错。

17. **"Rendered more hooks" 二次修复（漏改的 useEffect）**
  - 上次只把 `useMusic()` 提到顶部，**漏了 setShare 的 `useEffect` 仍在两个 early return 之后**（`if(isLoading)`/`if(playlist.length===0)`），加载态少调用该 hook → 报错复现。
  - 修复：把 `currentSong = playlist[currentIndex]`、`activeLyric`（IIFE 计算高亮行）与 setShare 的 `useEffect` 全部移到两个 early return **之前**（TopBanner/index.js 第 220-242 行），删除 early return 之后的重复块。现 MusicWidget 全部 hooks（useMusic + 4 个 useEffect）均在 early return 之前，符合 Hooks 规则。lint 0 错。
 