# 2026-08-01

## off-grid 节点群/离线消息改动态 + 输入框
- `src/pages/off-grid.js`：Hero 的静态 `ChatMockup` 升级为动态 `NodeChat` 组件。特性：消息自动滚到底；定时（4–8s）追加节点离线消息（带 typing 三点指示器）；底部 `<input>` + 发送按钮（对齐 chat 页绿色 `#07c160`，Enter 发送）；用户发送后追加"我"并触发节点自动回复。
- 语料库 `NODE_CORPUS`（50 条，覆盖上线/信号/位置/GPS/天气/加密/离线/中继/求救场景）+ `NODE_NAMES`（12 个节点代号）；放文件顶部，便于后期 AI 生成扩充。
- **AI 接入位**：`NodeChat` 内 `getNodeReply()` 已留注释位 `if (AI_ENABLED) fetch('/api/ai-reply', …)`，当前返回随机语料。
- **XinghuisamaBlogs 检查结论（更正）**：实际在 **E 盘根目录 `e:/XinghuisamaBlogs`**（非工作区）。内含可用 Gemini AI chat 接口：`my-blog-manager/app/api/chat/route.ts` 与 `XHBlogs/app/api/chat/route.ts`（Next.js edge，读 `GEMINI_API_KEY`/`OPENAI_API_KEY` 调 Gemini `gemini-2.5-flash-lite`）。线上可用域名 `https://www.xinghuisama.top/api/chat`（GET 探活 200，但 POST 实测空响应，疑似线上未配 key 或 Gemini 调用超时）。
- **AI 接入落地**：用户选"复用 XinghuisamaBlogs 线上接口 + 离网通信助手人格"。但因：①对方接口**无 CORS 头**（浏览器跨域必挂）；②对方 systemPrompt 写死"煤球猫娘"、不可改。故方案：
  1. 新建 `e:/my-forum/api/ai-chat.js`（Vercel edge 函数）：**服务端转发**到 `https://www.xinghuisama.top/api/chat`，规避 CORS；透传 `{reply}`；12s 超时；失败 502。
  2. `off-grid.js` 的 `getNodeReply` 改为先 `fetch('/api/ai-chat')`，**失败降级**到我方 `NODE_CORPUS` 大语料库（50 条）。
  3. 代理内预留**直连 Gemini 离网助手人格**开关：若 my-forum 的 Vercel 配 `GEMINI_API_KEY` 并取消 `if (process.env.GEMINI_API_KEY)` 注释，则走 `callGemini()` + `OFFGRID_SYSTEM_PROMPT`（简短、LoRa 节点口吻），人格按用户所选真正生效，且不依赖对方站点。
- 注意：对方线上 POST 实测不稳定（空响应），前端降级兜底是必须的；要稳定 AI 建议改用 my-forum 自带 key 直连。

## 集成 Meshtastic Web Flasher 到 /off-grid
- 仓库 `chinamesh/web-flasher` = Meshtastic 官方 Web Flasher 的中文 fork（Nuxt 3 SPA，依赖 Web Serial API 刷固件）。无法直接作为 React 组件移植。
- 实测 `https://flasher.meshtastic.org/` **国内可达**（HTTP 200，含 zh-Hans 简体中文 locale），且响应头**无 X-Frame-Options / 无 CSP frame-ancestors**（有 `ACAO:*`）→ 允许 iframe 嵌入。chinamesh 组织无独立部署域名，故直接 iframe 官方实例。
- 改动 `src/pages/off-grid.js`：`DeviceDialog` 由"静态兼容设备列表"改为**全屏 iframe 弹窗**，嵌入 `https://flasher.meshtastic.org/`（iframe 加 `allow="serial; bluetooth; usb"` 以启用 Web Serial）。原设备列表降为弹窗底部"可折叠兼容设备列表"参考区（复用 DEVICES 数据）。
- Hero 区"浏览设备"按钮文案改为"**设备刷写器**"（setDeviceDialogOpen 逻辑不变）。
- 注意：Web Serial API 需 HTTPS + 用户手势；我主站 monoblog.cc.cd 是 Cloudflare HTTPS，iframe 内可用。
- **修复 flasher iframe 被拦截（"已阻止此内容"）**：根因是 `docusaurus.config.js` 的 CSP meta 标签 `frame-src 'self'` 只允许同源 iframe。已改为 `frame-src 'self' https://flasher.meshtastic.org`。注意：此前 curl `-I` 抓 flasher 响应头无 X-Frame-Options，误判"允许嵌入"——实际拦截来自**父页 CSP**，非 flasher 本身。
- 体验优化：`DeviceDialog` 遮罩加 `onClick={onClose}`（点空白关闭）+ `overflowY:auto`，弹窗加 `maxHeight:90vh` 防小屏裁切需下滑才看得到。

## navbar / 页脚不显示 —— 彻底修复（改用官方组件，集成 meshtastic 渲染逻辑）

- **参考补丁库**：`E:/meshtastic`（同为 Docusaurus 站点）。其做法：**不 swizzle Navbar/Footer 主体**，navbar/footer 全部由 `docusaurus.config.js` 的 `themeConfig.navbar`/`themeConfig.footer` 配置 + Docusaurus 官方默认组件渲染；仅 swizzle `Navbar/MobileSidebar/{Layout,PrimaryMenu}`（带 Tailwind/英文，未移植，会破坏中文 dropdown）。
- **根因（对 2026-07-26 结论的再纠正）**：navbar/footer 不显示不是因为内联组件本身，而是**自定义 swizzle `src/theme/Navbar/index.tsx`、`src/theme/Footer/index.tsx` 覆盖了官方组件却渲染失败**。而项目其实**早已具备 meshtastic 的官方渲染基座**：`docusaurus.config.js` 的 `themeConfig.navbar/footer` 已完整引用 `siteData.json`；样式也齐全——`src/css/navbar.css`（meshtastic 完整 navbar 样式，官方类名 + hsl 变量）、`components.css`/`base.css`（官方 footer 样式）、`variables.css`（全部 hsl 变量含暗色）。这些都被自定义 swizzle 挡住了。
- **修复动作**：
  1. **删除** `src/theme/Navbar/index.tsx`、`src/theme/Footer/index.tsx` → 官方组件接管（= meshtastic 渲染逻辑）。
  2. **删除** 孤儿空目录 `src/theme/Navbar/MobileSidebar/`（0 文件死代码）→ 官方默认移动侧边栏接管（原生支持中文 dropdown 展开，无需 Tailwind）。未移植 meshtastic 的 MobileSidebar 补丁（英文 "Navigation" + Tailwind + 无法处理 dropdown，会破坏内容/排版）。
  3. **删除** 我一度新建的 `src/css/mono-navbar-footer.css`（多余，会与现有 meshtastic 样式冲突）。
  4. `docusaurus.config.js`：footer `copyright` 改为数组 `join('<br/>')`，把 `siteData.footerConfig.beian`（官方 footer 无 beian 字段）并入为 HTML `<a class="footer__beian-link">`；navbar 补 `logo`（读 `branding.logoSrc`）。
  5. `src/css/navbar.css` 末尾追加：三个绿色按钮 className 样式 `.navbar__link.navbar-contribute-btn/.navbar-chat-btn/.navbar-github-btn`（`#22c55e`/hover `#16a34a`，保留原绿色排版；navbar.css 原本只按 `[href*=github.com/meshtastic]` 选择器给样式，不认 my-forum 的 className）；`.footer__beian-link` 样式。
- **成果**：文本内容（`siteData.json` 的 items/links/beian）与视觉排版（现有 meshtastic CSS 体系 + 我的绿色按钮）保留；渲染逻辑集成 meshtastic（官方 Navbar/Footer）。
- **关键提醒**：`custom.css` 已 `@tailwind` + `@import navbar.css` 等；hsl 变量全在 `variables.css`。今后 navbar/footer 视觉调整应改 `navbar.css`/`components.css`/`base.css`，**不要**再 swizzle Navbar/Footer 主体（会重现“不显示”）。`src/theme/Layout/index.tsx` 用 `@theme-original/Layout`，与官方 Navbar/Footer 兼容，未动。

## 导航栏精简 + 新页面（/articles /more /off-grid）

- **需求**：原 navbar 项过多撑不开布局，精简为 5 项：博客、资源(下拉)、离网通信、更多、关于；右侧保留 +投稿/聊天/GitHub 三按钮。「更多」从下拉改为独立页跳转；「博客」跳 /articles 中转页（放博客/文章入口）。
- **改动 `src/data/siteData.json` 的 `navbarConfig.items`**：
  - 博客 `to:/articles/`（原为 /blog/，已移除独立「文章」「全部文章」项）
  - 资源 `type:dropdown`（资料下载/开源项目/开发工具，保留）
  - 离网通信 `to:/off-grid/`（新预留）
  - 更多 `to:/more/`（独立页，非 dropdown）
  - 关于 `to:/about/`
- **新建/重写页面**（均用 Docusaurus `Layout` + `@docusaurus/Link`，CSS 变量主题）：
  - `src/pages/articles.js`：原「全部文章」聚合页 → 改为导航中转页，两个大卡片入口：博客 `/blog/`、文章 `/docs/introduction/`。
  - `src/pages/more.js`：**新建**，独立「全站导航」页，分组展示：资源 / 工具箱(硬件监控、地震台网、代码片段、PCB、时光胶囊、排行榜、网易云下载) / 社区与更多(说说、友链、投稿、更新日志、系列项目、隐私、协议、RSS)。即原来「更多」下拉 + 资源/工具箱全部内容（除博客、文章）。
  - `src/pages/off-grid.js`：**新建**，离网通信占位页（用户说“先预留跳转链接”，内容建设中）。
- **注意**：原「工具箱」下拉已从 navbar 移除，其内容整体迁入 /more 页；原「全部文章」页面被复用改造为 /articles 中转页，原 Tag 聚合功能被覆盖（若需保留 Tag 聚合，应另建页）。

## 用户投稿合并进博客/文章列表（去掉“右侧单独一列”）

- **痛点**：之前某 AI 在 `BlogSidebar`/`DocSidebar` 里挂独立 `UserSubmissionsPanel`（带“用户投稿”标题 + ✍️ 前缀），形成与文章列表割裂的“右列”。用户要求投稿**混进列表、看不出区别**。
- **方案（数据层注入，最“看不出区别”）**：
  - **`src/theme/BlogListPage/index.js`（新建 swizzle）**：拉取 `user_submissions` published，用 `toBlogPost()` 映射成与官方 blogPost **同构**对象（`metadata.permalink:/submissions/?id=`、title、formattedDate、tags、authors、content=摘要 `<p>`、readingTime:undefined），与 `props.blogPosts` 合并后**按日期倒序**，传给 `OriginalBlogListPage`。官方 `BlogPostItem` 渲染 → 卡片样式完全一致，无任何“投稿”标记。分页仍由官方处理。
  - **`src/theme/BlogSidebar/index.js`**：移除独立 `UserSubmissionsPanel`，回归官方 `OriginalBlogSidebar`（不再右侧单列）。
  - **`src/theme/DocSidebar/index.js`**：投稿改为用官方 `.menu__link` 菜单项样式渲染（`<ul class="menu__list">` + `<Link class="menu__link">`），**无标题、无 ✍️ 前缀**，与文档树条目视觉一致，挂在官方侧边栏之后。
  - **删除** `src/components/UserSubmissionsPanel/index.js`（已无任何引用，旧“两列”逻辑残留）。
- **数据来源**：`supabase.from('user_submissions').select(...).eq('status','published')`，导入 `@site/src/supabase/supabaseClient`（与登录态共享客户端一致）。
- **取舍**：刻意**不加**任何“投稿”徽章/图标，严格满足“看不出一点区别”；后果是站主自己也无法从列表区分投稿与原创（用户明确接受）。
- **风险点**：`toBlogPost` 构造的 `metadata` 字段若与新版 Docusaurus `BlogPostItem` 期望有出入，可能运行时报错（dev 热更新可验证）；如报错需补 `metadata` 字段（如 `description`/`editUrl`）。

## 与中国无关的地震速报 → 左下角圆形图标

- **诉求**：国外（非中国）地震速报原来在 `WarningModal` 里是**右下角 340px 小卡片**，占空间干扰；要求缩小为**左下角圆形图标**，避免干扰。
- **位置**：`src/components/WarningModal/index.js`。原有 `isDomestic()` 判定（经纬度落在中国范围 18–54°N / 73–135°E，或区域/标题含国内关键词）；`fullscreen`=国内地震+非地震预警（全屏弹窗不变）；`foreignQuakes`=`isEq && !isDomestic`（原 `minor` 变量，已改名）。
- **改动**：`foreignQuakes` 不再渲染右下角卡片，改为 `fixed left:16 bottom:16` 的 **52px 圆形按钮**（白底橙边 🌍，多条时右上角数量角标 `position:relative` 修正定位）。新增 `quakeOpen` state：点击圆形按钮 toggle，在按钮**上方**（bottom:64）展开一个 `maxHeight:70vh` 可滚动面板，列出国外地震详情卡片 + 逐条“知道了”/“关闭全部”。国内地震与非地震预警维持全屏不变。
- **注意**：圆形图标仅作轻提示，不自动展开，不打扰阅读；角标定位依赖按钮 `position:relative`。

## 资料下载页改为 meshtastic download 风格

- **需求**：把 `src/pages/downloads.js`（我方）换成 `E:/meshtastic/src/pages/downloads/index.tsx` 的视觉风格。meshtastic 风格=**分区块横向卡片布局**：每个区块左侧 `w-1/5` 渐变竖条(from-xxx-500 to primary)+大图标居中，右侧 `bg-primary` 容器内并排 `.card`（card__header/body/footer，底部绿色 accent 按钮），圆角 `rounded-xl`。
- **实现（复刻布局，不用 meshtastic 依赖）**：meshtastic 原页用 TSX + `@heroicons/react` + `react-icons` + `swr` + Tailwind 主题色（bg-primary/secondary），我方是 JS 项目且无这些依赖，故**不复用其源码/依赖**，只复刻布局与配色：
  - 数据仍取自我方 `src/data/Download.json`（`resourceList` 按 `item.type` 分组，`categoryMap` 取分类名），文本/资源全部保留。
  - 每个分类一个 `<section>`：`display:flex; overflow:hidden; borderRadius:14`；左侧 `width:20%` 竖条用内联 `linear-gradient`（各分类配色：tutorial绿/tool橙/source紫/asset粉/hardware青）+ emoji 大图标(48px)居中；右侧 `flex:1` 浅底容器内 `flexWrap` 并排 Infima `.card`（白卡、`border`、card__header/body/footer），底部按钮用我方绿色 `#22c55e`（hover brightness），无 url 显示灰色"暂无资源"。
  - 用 Infima 类 `container`/`card`/`card__header`/`card__body`/`card__footer`/`margin-vert--lg`，配少量内联样式定色，无需 Tailwind 主题色或新依赖。
- **取舍**：图标用 emoji 而非 Heroicons（避免装包）；配色选用 meshtastic 的绿色调呼应我方绿色按钮基色 `#22c55e`，但分类渐变用多彩色区分（贴近 meshtastic 多彩分区）。

## 复用 meshtastic 的 docs/blog 文章列表格式

- **核查结论**：meshtastic 的 blog 列表格式 = Docusaurus **官方默认**卡片样式。其 `src/theme/BlogPostItem/index.tsx` 只是 wrapper（调官方 `BlogPostItem` + 追加 `Comments` 组件），**未改卡片外观**；`css/*.css`（variables/base/navbar/components）也**完全不涉及** `.blogPostItem*` / 文档列表卡片。docs 列表同样纯官方（无 DocItem/DocSidebar swizzle 改样式）。
- **我方现状已对齐**：我方无任何自定义博客/文档卡片 CSS；博客卡片走官方 `BlogPostItem`，DocSidebar 投稿项用官方 `.menu__link` 类 → 与 meshtastic 视觉一致。无需复制其 TSX（且其 BlogPostItem 引用 `@site/src/components/Comments` 我方无此组件，照搬会渲染失败）。
- **为"确保显示正常"做的加固**：`src/theme/BlogListPage/index.js` 的 `toBlogPost()` 补全 metadata 字段，避免官方 `BlogPostItem` 访问 undefined 报错：`readingTime` 改占位对象 `{words:0,minutes:0}`（原 undefined）、`authors` 加 `key`/`title`/`imageURL`、`metadata` 加 `key`/`hasTruncateMarker:false`/`editUrl:undefined`、`description`。投稿以官方卡片样式混入，看不出区别。
- **结论**：复用任务=确认对齐官方默认格式 + 加固投稿卡片健壮性，已达成；本地 `pnpm start` 刷新 /blog/ 与 /docs/* 验证即可。

## 编写离网通信页 /off-grid（复刻 meshtastic 主页）

- **需求**：新建 `src/pages/off-grid.js`，复用 `E:/meshtastic` 主页（`src/components/homepage/*`）**所有元素与结构**（Hero+三按钮、EcosystemStats、HomepageFeatures、HomepageDownloads、设备弹窗、社交侧栏、网络地图背景、赞助商），文字改中文/自由发挥，**字体/视觉不变**（等宽 hero、圆角卡片、绿色主色）。
- **为何不复用 meshtastic 源码**：其首页依赖 `@/components/ui/button`、`lucide-react`、Popover/Tooltip、`@/hooks/use-number-animation`、`@/data/*`、SVG 资源——我方均无（且 node_modules 空），照搬会渲染失败（同"navbar 不显示"根因）。故**复刻视觉+结构，替换依赖**：内联 SVG 图标（lucide 形状）、`useCountUp` 自写数字动画、设备/设备弹窗/社交用文本+内联样式、网络地图背景用 CSS 点阵、聊天界面用 CSS mockup。
- **视觉对齐关键**：meshtastic 靠 hsl 变量（`--btn-primary`/`--background`/`--foreground`/`--card`/`--border`/`--muted-foreground`/`--accent`/`--popover` 等）。我方 `tailwind.config.ts` 的 `colors` **已映射**这些语义名到 `hsl(var(--...))`，但我方 `variables.css` 没有这些变量（主站用 `--ifm-*` 体系）。**解法**：新建 `src/css/off-grid.css`，在 `.off-grid-page` 作用域内定义 meshtastic 的 hsl 变量（浅色 + `[data-theme="dark"] .off-grid-page` 暗色），并用原生 CSS 写社交侧栏/移动横条/地图点阵/弹窗遮罩（不依赖被禁的 Tailwind corePlugins：container/backdropBlur/position 均 `false`，但原生 CSS 不受影响）。`@import` 进 `custom.css`。
- **细节**：`<main className="container">` 无效（corePlugins.container:false），已用内联 `maxWidth:80rem;margin:0 auto` 兜底；Hero 两列用 `grid grid-cols-1 md:grid-cols-2`（Tailwind 生成）；社交侧栏 fixed 桌面端、移动横条 `<1280px` 显示。`Layout` 仍套主站 navbar/footer（符合整体导航保留设计）。
- **验证**：lint 通过；本地 `pnpm start` 开 `/off-grid/` 验证。注意 Docusaurus SSR 下 `useCountUp` 的 rAF/performance 仅在 useEffect 内（客户端）执行，SSR 安全。
